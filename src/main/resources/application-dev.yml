# DEV profile

#logging:
#  level:
#    root: debug
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev
    username: sfg_dev_user
    platform: mysql
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    Need to generate init database scripts
#
#    properties:
#      hibernate:
#        hbm2ddl:
#          delimiter: ";" # To insert ; after each SQL line generated by the hbm2ddl. By default, nothing
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: guru_database_create.sql